const NO_DOCUMENT = 'There is No document in the database!';

const DOCUMENT_DELETED = 'Document deleted from the Database';

const EMAIL_EXISTS = 'User with this emailId already exists';

module.exports = {
  NO_DOCUMENT,
  DOCUMENT_DELETED,
  EMAIL_EXISTS,
};
